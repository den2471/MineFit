services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PROJECTS_ENDPOINT=https://api.modrinth.com/v2/projects
      - VERSIONS_ENDPOINT=https://api.modrinth.com/v2/versions
